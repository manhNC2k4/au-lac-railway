# progress.md — Nhật ký tiến độ 30 giờ

**T0 = <điền lúc bắt đầu>** · Mọi giờ ghi dạng `H+xx` (tính từ T0).

## Luật ghi (đọc 1 lần, tuân thủ 30 giờ)

1. **Xong mục nào → append NGAY.** Không để cuối ngày. Không ghi = coi như chưa xong.
2. **Append xuống cuối. KHÔNG sửa dòng người khác.** Sửa dòng mình thì được (WIP → DONE).
3. **`✅ DONE` phải có bằng chứng chạy được**: lệnh test + output, commit sha, screenshot path, hoặc URL. *"Xong rồi"* không phải bằng chứng.
4. **`⛔ BLOCKED` phải ghi chờ ai / chờ cái gì.** Block > 30 phút ⇒ báo BE1 ngay, đừng ngồi chờ.
5. **Đổi contract** ⇒ dòng `⚠️ CONTRACT CHANGE` + impact list + tag owner liên quan. **BE1 phải duyệt.**
6. **Checkpoint (H2, H6, H10, H14, H18, H23, H26, H30): MỌI dev ghi 1 dòng**, kể cả khi không có gì mới.

Trạng thái: `✅ DONE` · `🚧 WIP` · `⛔ BLOCKED` · `⚠️ CONTRACT CHANGE` · `❌ CUT` (bỏ, ghi lý do)

---

## Nhật ký

| Giờ | Dev | Mục | Trạng thái | Bằng chứng | Unblock ai |
|-----|-----|-----|-----------|-----------|-----------|
| H+00 | — | Kickoff | 🚧 WIP | 5 file plan đã đọc | — |
| H+?? | BE2 | forecast.json schema + bid-price approximation | ✅ DONE | `src/forecast/{network,bid_price,forecast}.py` · `python -m unittest tests.test_bid_price -v` → 4 passed | BE1 (đối chiếu schema), BE3 (so bid trong offer) |
| H+?? | BE2 | seed/backtest/events-seed-{5}.jsonl (NHPP từ YAML, không dùng dataset thô) | ✅ DONE | `python -m src.backtest.events` → 5 file trong `seed/backtest/`, ~400 request/seed, `seed/backtest/checksums.json` | BE1 (duyệt & commit chính thức vào seed/), FE2 (S04) |
| H+?? | BE2 | backtest engine: baseline B0 (no-gap-reuse) vs Âu Lạc (seat-matrix + bid guardrail), common random numbers, metrics | ✅ DONE | `python -m unittest tests.test_backtest -v` → 7 passed, gồm `test_baseline_rejects_golden_request` ⭐ và `test_no_ground_truth_import` (CI gate) | FE2 (S04 backtest comparison) |
| H+?? | BE2 | Ghi chú phạm vi: backtest dùng `fixed_fare` chung cho cả 2 chính sách (cô lập biến allocation, chưa cắm PricingEngine thật của BE3) | ⚠️ CONTRACT NOTE | Xem docstring `src/backtest/engine.py` — khi BE3 xong PricingEngine (H10-H14), thay fixed_fare bằng giá AI thật, interface accept/reject+revenue giữ nguyên | BE3, BE1 (tích hợp) |
| H+00 | BE1 | requirements.txt + openapi.yaml freeze | ✅ DONE | `backend/requirements.txt`, `backend/openapi.yaml` — 11 endpoint, parse OK (`python -c "yaml.safe_load(...)"` → paths=11) | BE2,BE3,FE1,FE2 |
| H+00 | BE1 | seed/ prior + calibration (scenario/fare/policy/forecast) | ✅ DONE | `backend/scripts/build_seed.py` chạy thật trên `generated_data/data/` — golden gap `C01-S017` FREE segment [3,4] verified qua `pytest tests/test_seed.py -v` → 3 passed | BE2,BE3,FE1,FE2 |
| H+00 | BE1 | DB schema xác nhận | ✅ DONE | `docker compose up -d db flyway` → V1+V2 migrate OK | — |
| H+00 | BE1 | SeatStateManager + atomic CAS hold/confirm | ✅ DONE | `backend/src/state/seat_state_manager.py` — `pytest backend/tests/ -v` trên Postgres thật → **12 passed** (phần BE1), gồm `test_two_competing_holds_one_wins` | BE2,BE3,FE1,FE2 |
| H+00 | BE1 | FastAPI app (11 route thật, không stub) | ✅ DONE | `backend/src/api/` — smoke thật qua uvicorn: reset→offer(ACCEPT)→hold(ACTIVE)→confirm(CONFIRMED, giá không đổi)→seatmap segment 3,4 chuyển SOLD | FE1,FE2 |
| H+00 | BE1 | CI gate `_ground_truth` | ✅ DONE | `grep -r "_ground_truth" src/` → rỗng | — |
| H+00 | BE1 | Ghi chú phạm vi (solo session) | ⚠️ CONTRACT CHANGE | `/offers` pricing/bid dùng công thức rút gọn tạm thời (chưa cắm forecast module BE2 thật, chưa CSXH/guardrail-ceiling-exceed case của BE3) — xem `backend/src/api/routes_offers.py` docstring. Khi BE2/BE3 vào, thay logic tại đây, KHÔNG đổi route/schema. | BE2 (forecast thật), BE3 (pricing/CSXH thật) |
| H+?? | BE2 | `forecasts/refresh` logic (`forecast.refresh_forecast`, bump version, giữ run_id/che_do_gia) — route BE1 gọi | ✅ DONE | `python -m unittest tests.test_backtest` → 14 passed, gồm `test_refresh_bumps_version_keeps_run_id` | BE1 (cắm vào route `POST /demo/forecasts/refresh` trong src/api/) |
| H+?? | BE2 | Evidence runner H14-H18: `engine.load_events`/`load_all_events` đọc `seed/backtest/*.jsonl` đã commit + `python -m src.backtest.engine` xuất report median+min/max+raw+checksum | ✅ DONE | `python -m src.backtest.engine` → 5 seed, 0 failed, baseline median 17.807.000đ vs Âu Lạc 44.725.000đ, checksum `daae2f6f…`; `test_committed_events_match_generator` xác nhận jsonl commit khớp generator | FE2 (S04 lấy report thật), BE1 (`GET /backtests/{id}` trả report này) |
| H+00 | BE1 | ⚠️ **HỢP NHẤT `seed/` sau conflict discard+pull**: `backend/seed/backtest/` của tôi (placeholder rỗng) và `seed/backtest/` root của BE2 (data thật) từng tồn tại song song — dời data thật của BE2 vào `backend/seed/backtest/`, xoá `seed/` root, sửa `backend/src/backtest/events.py` (`REPO_ROOT/"seed"` → `BACKEND_ROOT/"seed"`, thêm hằng `BACKEND_ROOT = parents[2]`) | ✅ DONE | `pytest backend/tests/ -v` sau hợp nhất → **23 passed** (11 BE2 + 12 BE1, không hỏng cái nào); sửa thêm `tests/test_state_cas.py` import `from conftest import` → `from tests.conftest import` (do `tests/__init__.py` của BE2 đổi pytest import mode) | BE2 (xác nhận `seed/` giờ nằm dưới `backend/seed/`, không phải root nữa) |
| H+?? | BE3 | merging resolver: `continuous_same_seat` (numpy, FREE=0/SOLD=1/HELD=2 khớp SSM contract) + `reused_gap` label + ranking (reused-first) + protected filter | ✅ DONE | `python -m unittest tests.test_merging -v` → 6 passed, gồm `test_golden_gap_found` (C01-S017 THO→DHO seg[3,4]), `test_sold_bookings_never_moved` (read-only, 0 mutation), `test_resolver_under_200ms` | FE1 (S02 visualize reused_gap), BE1 (adapter vị trí 2 resolver) |
| H+?? | BE3 | PricingEngine: F0 (giá gốc O-D, KHÔNG cộng leg) → luật động **YAML khai báo** (`rules/pricing_rules.yaml`) → guardrail đúng thứ tự → CSXH `max` áp SAU cùng. `PricingContext`/`SafetyContext` **tách biệt bằng type** | ✅ DONE | `python -m unittest tests.test_pricing -v` → 11 passed, gồm 3 test ⭐ lên slide (`test_price_invariant_to_repeated_search`, `test_price_locked_after_hold`, `test_pricing_features_exclude_sensitive`) + `test_social_policy_discount_is_max_not_product` + `test_social_policy_applied_after_dynamic` + `test_guardrail_order_floor_ceiling_delta_round_freeze` + `test_policy_unavailable_returns_503` | FE1 (S06 compliance), FE2 (pitch), BE1 (adapter vị trí 3 pricing) |
| H+?? | BE3 | OfferService: pipeline `seat plan→base fare→price→guardrail→so bid→offer`, Offer immutable + expiry + 4 versions, **KHÔNG giữ ghế**, DecisionRecord append-only (input_hash+versions+rules_fired+violations+explanation = nhật ký quyết định/XAI) | ✅ DONE | `python -m unittest tests.test_offer -v` → 5 passed (golden ACCEPT trên C01-S017, REJECT khi bid > giá, holds-no-seat, decision-record audit). Tổng BE2+BE3: `python -m unittest tests.test_bid_price tests.test_backtest tests.test_merging tests.test_pricing tests.test_offer` → **36 passed**; CI gate `grep -r _ground_truth src/` rỗng | BE1 (cắm 3 module vào `src/api/routes_offers.py` thay logic rút gọn — KHÔNG đổi route/schema) |
| H+?? | BE3 | ⚠️ **CONTRACT NOTE cho BE1**: 3 module đặt tại `backend/src/{merging,pricing,offer}/` + luật tại `backend/rules/*.yaml` (khớp Docker context của BE2). Chưa cắm vào route — BE1 tích hợp ở H10-H14 theo Master §8. Interface: `resolver.best_same_seat(matrix,seat_ids,seg_from,seg_to)`, `PricingEngine(policy).price(f0,ctx,safety)`, `OfferService(engine,products,versions).build_offer(...)`. `bid_by_segment` do BE2 cấp (`forecast.bid_price`), OfferService chỉ **so sánh** final fare vs Σbid (không tự tính bid) | ⚠️ CONTRACT NOTE | Chưa đụng `routes_offers.py`/`schemas.py`/`seed/`/`state/` của BE1, `forecast/`·`backtest/` của BE2 — 0 file người khác bị sửa. `docker-compose.yml` cần mount `../rules` như đã mount `seed` (BE1 tự quyết, giống cảnh báo seed/ trước) | BE1 (integration + volume mount `rules/`) |

| H+?? | BE1 | T1 (BE_INTEGRATION_PLAN): Dockerfile CMD `main:app` → `src.api.main:app` (container backend từng crash lúc start); xác nhận seed/+rules/ đã nằm trong build context, KHÔNG cần mount — cảnh báo cũ của BE2/BE3 lỗi thời | ✅ DONE | `backend/Dockerfile` dòng 12 | FE1,FE2 (chạy full stack Docker) |
| H+?? | BE1 | T2+T3: cắm 3 module BE3 (`merging.resolver`, `PricingEngine` YAML+CSXH, `OfferService`+DecisionRecord) + bid `forecast.bid_price` & `forecast.refresh_forecast` BE2 vào `routes_offers.py`/`routes_demo.py` — thay TOÀN BỘ logic rút gọn, route/schema giữ nguyên | ✅ DONE | smoke: offer THO→DHO trả `rules_fired=[R_MUA_VU_HE2026,R_SAT_NGAY,R_AI_LINH_HOAT,R_GIO_CHOT]`, refresh bump version 2→3 giữ run_id, analytics trả forecasts+bid/leg | FE1 (S01/S05), FE2 (S03) |
| H+?? | BE1 | T4: 3 route còn thiếu `POST /backtests`, `GET /backtests/{id}`, `GET /decisions/{id}` → 11/11 endpoint khớp openapi.yaml | ✅ DONE | `python -c "...app.routes"` → 11 path; GET /decisions trả versions+breakdown+violations+audit_timeline | FE1 (S05), FE2 (S04) |
| H+?? | BE1 | T5: backtest thay `fixed_fare` bằng giá thật (baseline=giá niêm yết F0, Âu Lạc=PricingEngine) qua `make_priced_fare_fns` — interface giữ nguyên, default cũ giữ cho test BE2 | ✅ DONE | `python -m src.backtest.engine` → 5 seed 0 fail, baseline median **19.531.000đ** vs Âu Lạc **49.958.000đ** (+156%), checksum `c622c3f2…` — SỐ CHỐT cho S04/pitch | FE2 (S04, pitch) |
| H+?? | BE1 | T6: gộp 5 checksum backtest vào `expected_checksums.json` (+build_seed.py); thêm rule `R_GIO_CHOT` (lead=0, ×1.7) tạo case VƯỢT TRẦN thật — golden path chạm `TRAN`, guardrail clamp | ✅ DONE | `pytest tests/test_seed.py` 3 passed; offer golden `violations=['TRAN']`, `clamped=true` | FE1 (S06 hiển thị 0 vi phạm/clamp) |
| H+?? | BE1 | T6b: resolver ranking đổi sang BEST-FIT (ít ô FREE thừa ngoài span nhất trước) — spec cũ (reused-first + seat_id) trả C01-S007 (trống 1–4) thay vì golden C01-S017; best-fit khớp mục tiêu "giữ ghế trống dài cho chặng dài" và DoD "tìm ĐÚNG C01-S017" | ⚠️ CONTRACT NOTE | `tests.test_merging` 6 passed không đổi; smoke chọn đúng C01-S017 seg[3,4] `reused_gap=true` — BE3 rà lại nếu quay lại phiên | BE3 (review ranking) |
| H+?? | BE1 | T7+T8: overview số thật (revenue/pax-km/empty-seat-km/recent decisions) + evidence run: golden E2E **3/3** (reset→offer C01-S017 ACCEPT→hold 2 leg→confirm giá không đổi 456.000đ→seatmap SOLD→decision trace→overview đổi số), reset deterministic checksum `8c8ef3c7…` | ✅ DONE | smoke 3/3 tổng 1.6s; **48/48 test pass** trên Postgres thật (V1+V2, volume dựng lại sạch vì pgdata cũ là PG16); NFR: offer 0.09–0.15s <1s, reset 0.2s <3s, resolver <200ms (test BE3); CI gate `grep -r _ground_truth src/` rỗng | Cả đội — backend feature complete, chờ FE |
| H+?? | INT | MODEL_BASE §P5 (port ghép nhiều ghế): `merging/resolver.py` += `resolve_multiseat_options`+`MergedSeatPlan`+`best_multiseat` (port từ `integration/resolver_multiseat.py`) — loại trừ hành khách ưu tiên, cổng dwell ≥5', greedy min-cover, tất định, read-only | ✅ DONE | `python -m pytest tests/test_merging.py tests/test_adapter.py -q` → **15 passed**; commit `81d7a19`. Phần DB (schema `seat_plan[]` nhiều leg + `hold_multi` CAS multi-set + wiring route + FE consent) **CHƯA làm** — cần Postgres + BE1 duyệt | BE1 (schema+CAS multi-set, ⚠️ CONTRACT CHANGE), BE3 (gọi fallback khi same-seat rỗng), FE (disclosure consent) |
| H+?? | INT | MODEL_BASE §P0.4 (adapter duy nhất): `adapters/model_adapter.py` — span 1-based inclusive ⇄ cột 0-based nửa mở (golden [3,4]⇄[2,4)), seat_id⇄index, seatmap DB⇄ma trận int8 (ô thiếu=SOLD fail-safe); `routes_offers._matrix_from_seatmap` + `STATE_CODE` chết dồn về adapter | ✅ DONE | `python -m src.adapters.model_adapter` self-check OK; `test_adapter.py` 4 passed; commit `81d7a19` | BE1/BE3 (dùng chung adapter, cấm convert tay trong route §3.4) |
| H+?? | INT | MODEL_BASE §P0.6/P8 (sweep số): `scripts/audit_constants.py` — 2 CI gate (`_ground_truth`, `import pandas`) + quét literal thiếu `# nguồn:` (AST, bỏ qua demo/test scaffolding) | ✅ DONE | `python scripts/audit_constants.py` → 2 gate **OK rỗng**, exit 1 vì 62 literal runtime chưa gắn nguồn = backlog P8 cả đội; code mới của INT 0 vi phạm; commit `81d7a19` | Cả đội (P8: gắn `# nguồn:` cho 62 literal còn lại) |
| H+?? | INT | ⛔ MODEL_BASE §P1/P6 (seed hiệu chuẩn + backtest từ dataset) — **BLOCKED**: không có `generated_data/data/*.parquet` local. §P2/P3/P4 (bid DLP/pricing elasticity/forecast DemandModel) — **BLOCKED**: `app/bt3` import pandas ở module-level, đụng bất biến "no pandas in runtime" (§3.3 vs §3.5), chưa cài pandas/sklearn/scipy | ⛔ BLOCKED | Cần: (P1/P6) chạy `generate_data.py` sinh dataset; (P2/P3/P4) quyết định lazy-import pandas trong `app/` + pin deps đã train artifact | Chủ plan (quyết hướng xử lý pandas), seed-extractor (dataset) |
| H+?? | seed-extractor | MODEL_BASE §P0.1 unblock: `generate_data.py` đã chạy xong, dataset có local | ✅ DONE | `generated_data/data/` — `stations.csv`, `trains.csv`, `seat_inventory.csv`, `refunds.parquet`, `calendar_events.csv`, `manifest.json`, `search_log/`, `transactions/` (380M, chạy 17/07 18:52-19:23) | INT/BE2 (P1 seed hiệu chuẩn + P6 backtest số thật hết blocker dataset — P2/P3/P4 vẫn blocked vì pandas module-level trong `app/bt3`) |
| H+?? | INT | MODEL_BASE §P1 (A1/A2/A3/A4/A5) — seed hiệu chuẩn từ dataset thật, hết số bịa: `backend/scripts/calibrate_seed_from_dataset.py` (mới) tính `target_occ`+`intensity` từ `transactions`+`search_log` thang=2026-06 SE1 LE (15 ngày quanh 06-15) + `confidence`=1-MASE_model/2 (nguồn `models/artifacts/bt1_feature_spec.json`) + `rho_t` đọc từ `trains.csv` (=1.1, khớp hardcode cũ) → ghi `calibration_cache.json`; `build_seed.py` đọc cache thay `target_occ` hardcode + `forecast×0.6` phẳng + `confidence=0.85` gõ tay; xoá dead code `build_backtest_placeholders` | ✅ DONE | `python backend/scripts/calibrate_seed_from_dataset.py` → occ/intensity thật (seg7 DNA-SGO ế nhất 0.156, seg6 HUE-DNA nghẽn nhất 0.669 — khác hẳn profile bịa cũ seg3 bottleneck/seg6 ế); rebuild seed OK, golden gap C01-S017 seg[3,4] vẫn giữ; `pytest backend/tests/ -q` → **79 passed** | BE1 (checksum seed đổi — báo FE fixture đổi), BE2 (rà lại profile occupancy mới trong pitch/S01) |
| H+?? | INT | MODEL_BASE §P6 (A10/A11) — backtest event stream + evidence hết số bịa: `backend/scripts/calibrate_backtest_lambda.py` (mới) tính λ thật per O-D (8 ga golden) từ `search_log` thang=2026-06 SE1 08..22/06, scale 40/448 (thay `TARGET_TOTAL_REQUESTS=400` bịa) → `backtest_lambda_cache.json`; `src/backtest/events.py` đọc cache thay công thức `E_u_ngay×weight`, `HORIZON_DAYS` 90→**65** (nguồn `calendar_events.csv` ngày=2026-06-15 `H_horizon`, đợt HE_2026), xoá `_pair_weight` dead code | ✅ DONE | Regenerate 5 seed event + `build_seed.py` (checksum mới) → `pytest backend/tests/ -q` **79 passed**; `python -m src.backtest.engine` → baseline median **18.848.000đ** vs Âu Lạc **23.396.000đ** = **+24.1%** (KHÔNG PHẢI +156% cũ — số cũ là artifact kịch bản baseline-từ-chối-gần-hết, không phải backtest calibrate thật) | FE2 (⚠️ ĐỔI SỐ PITCH/S04: +156%→+24.1%, cùng median/min/max mới), BE1 (checksum backtest đổi trong `expected_checksums.json`) |
| H+?? | INT | P2/P3/P4-Bước1 (live-import `app/`): xác nhận CI gate `audit_constants.py` chỉ quét `backend/src/` (`SRC = backend/scripts/../src`), KHÔNG chặn `app/` → live-import hợp lệ, không sửa gate; chốt kiến trúc import trực tiếp `app/` vào backend process (không subprocess). `backend/docker-compose.yml` build context đổi `..` (repo root) + `dockerfile: backend/Dockerfile`; root `.dockerignore` mới (loại `generated_data/,demo/,web/,plan/,plans/,docs/,eval/,tests/,integration/,.git/`); `backend/Dockerfile` copy `app/`+`models/artifacts/`+`backend/`, `ENV PYTHONPATH=/app:/app/backend`; `backend/requirements.txt` += `scikit-learn==1.9.0` (khớp CHÍNH XÁC version train `bt1_forecast_hgb.joblib`, verified qua đọc metadata `sklearn_version` trong pickle binary) + `joblib` | ✅ DONE | `docker compose up -d --build` build OK, sklearn cài đúng 1.9.0; smoke `curl localhost:8000/api/v1/demo/overview?...` → HTTP 200; `pytest backend/tests/ -v` chạy HOST (không phải container — `generated_data/` cố tình không ship vào image theo bất biến "dataset never connects to runtime") → **79 passed** | INT (Bước 2: verify adapter, Bước 3+: boot sequence nạp artifact + P2/P3/P4 thật) |
| H+?? | INT | P2/P3/P4-Bước2: verify `backend/src/adapters/model_adapter.py` còn đúng (chỉ verify, không sửa) — `span_to_cols(3,4)`→`slice(2,4)` golden `[3,4]` 1-based inclusive ⇄ `[2,4)` 0-based half-open, đối chiếu `app/bt2_ssm.py:51` (`hi-lo` cùng convention 0-based half-open) khớp | ✅ DONE | `pytest backend/tests/test_adapter.py -v` → **4 passed** | INT (Bước 3: boot sequence nạp artifact `deps.py`) |
| H+?? | INT | P2/P3/P4-Bước3: boot sequence nạp Pricer+DemandModel (live-import `app/`) — `backend/src/api/deps.py` += `load_models()` (gọi `app.bt5_pricing.Pricer.load(use_elasticity=True)` + `app.bt1_forecast.DemandModel.load()`, thêm repo root vào `sys.path` cho import `app.*`), `get_pricer()`/`get_demand_model()` fail-closed → `PolicyUnavailable` (503) khi artifact thiếu/lỗi (KHÔNG raise trong `load_models`, chỉ log+None); `backend/src/api/main.py` += FastAPI `lifespan` gọi `load_models()` 1 lần lúc boot. Cài `scikit-learn==1.9.0`+`joblib` trên host (trước chỉ có trong docker) | ✅ DONE | `pytest backend/tests/test_model_boot.py -v` → **5 passed** (happy path nạp thật + 2 case fail-closed mock raise); full suite host **84/84 passed**; docker rebuild+restart → log "Application startup complete" (không exception), curl overview vẫn 200 | INT (Bước 4: P2 bid=DLP thật) |
| H+?? | INT | ⚠️ **CẢNH BÁO cho bước sau** (chưa sửa — ngoài scope Bước3): `tests/test_api_e2e.py` dòng 68 `client = TestClient(app)` module-level, KHÔNG dùng `with` → verified bằng thực nghiệm Starlette KHÔNG trigger `lifespan` (startup event không chạy). Vô hại ở Bước3 (chưa route nào gọi `get_pricer()/get_demand_model()`), nhưng Bước4/5 khi wire vào `routes_offers.py` sẽ làm e2e test 503 giả nếu không sửa | ⚠️ CONTRACT NOTE | Bước4/5 cần sửa `test_api_e2e.py` dùng `with TestClient(app) as client:` hoặc gọi `deps.load_models()` thủ công trong fixture trước khi test route pricing/bid | INT (tự làm ở Bước4) |
| H+?? | INT | Bước4 (P2 bid=DLP thật) — scouting xong, **BLOCKER kiến trúc mới phát hiện**: `app.bt3_allocation.analyze_run(ssm,pricer,chuyen_id,forecast)` đọc thẳng PRIVATE attrs của `app.bt2_ssm.SeatStateMatrix` (`ssm._span[chuyen_id]`, `ssm.st.ga_id`/`ssm.st.ly_trinh_km` — pandas DataFrame từ `stations.csv`) + method `.get_state()/.get_segment_meta()/.load_factor()/.seg_range()`. `SeatStateMatrix.__init__` tự build ma trận RỖNG mới từ `generated_data/data/*.csv`, KHÔNG liên quan Postgres runtime state | 🚧 WIP | Chưa code. Cần viết shim class thay `SeatStateMatrix` (implement cùng interface + `.st`/`._span`) nạp từ Postgres snapshot 8-ga/40-ghế/7-leg golden qua `model_adapter.py` — module mới thật sự (backend/src/allocation/ssm_shim.py hoặc tương tự), không phải wiring đơn giản. Khớp ước lượng gốc plan "~2-3 ngày-người" | Cần xác nhận hướng đi trước khi code (effort lớn hơn 1 bước nhỏ) |
| H+?? | INT | Bước4 — **CHỐT vị trí shim**: `.st` là `pandas.DataFrame` bắt buộc (khớp `bt3_allocation.py` đọc `ssm.st.ga_id`) → file build shim PHẢI `import pandas` → đặt trong `backend/src/` sẽ dính CI gate. Quyết định: đặt `integration/ssm_from_postgres.py` (KHÔNG phải `backend/src/`), `backend/src/allocation/cache.py` chỉ import module đó (không có literal `import pandas` trong `backend/src/` → qua gate). Khớp pattern đã có: multiseat resolver cũng port từ `integration/resolver_multiseat.py`. Tinh thần bất biến giữ nguyên về chữ (gate check text); pandas THỰC THI trong request qua `app.bt3_allocation` là hệ quả đã chấp nhận khi chốt live-import trực tiếp (không phải vi phạm mới) | ⚠️ CONTRACT NOTE | Chưa code — quyết định kiến trúc, user xác nhận | INT (code shim `integration/ssm_from_postgres.py` + `backend/src/allocation/cache.py` bước tiếp) |
| H+?? | INT | **Bước4 DONE** (P2 bid=DLP thật): `integration/ssm_from_postgres.py` (mới, shim `PostgresSeatStateMatrix` — nhận `matrix` đã convert sẵn qua `model_adapter`, KHÔNG tự import `backend.src.*` để tránh nạp trùng package qua 2 đường sys.path; `chuyen_id` nội bộ = `"{train_id}_{service_date}"` khớp `rsplit('_',1)` của `bt3_allocation`/`Pricer.f0` để tra đúng `rho_t["SE1"]`); `backend/src/allocation/cache.py` (mới, `refresh()`/`get()` cache theo `(service_run_id,matrix_version,forecast_version)`, refresh gọi từ `routes_demo.py` reset+forecast-refresh, KHÔNG mỗi request; forecast O-D DLP xấp xỉ từ forecast MỨC ĐOẠN của seed — mỗi đoạn liền kề = 1 cặp O-D, cặp khác cầu=0, bảo thủ không bịa). `routes_offers.py` step5 đọc `allocation_cache.get(...)`, miss → 503 `POLICY_UNAVAILABLE` KHÔNG fallback; `routes_demo.py` analytics cũng đổi sang đọc cache (bỏ `bid_price.bid_price_segment` xấp xỉ cho display). **KHÔNG xoá** `forecast/bid_price.py` — vẫn được `backend/src/backtest/engine.py` dùng (offline replay riêng, không phải live path); sửa docstring/CLAUDE.md phản ánh đúng vai trò mới. Sửa bug có sẵn `test_api_e2e.py` dòng 68 (`TestClient(app)` module-level không trigger lifespan → `deps.load_models()` không chạy → 503 giả) bằng `__enter__()`. 2 golden-value assertion cũ (`bid_total_vnd > 0`) sửa thành `>= 0` + giải thích: seg 3-4 golden gap có cầu (3.6/16.2) < sức chứa còn lại (19/28) ⇒ DLP dual=0 hợp lý (không nghẽn), khác công thức scarcity cũ luôn >0 — ACCEPT vẫn đúng vì đến từ trần giá | ✅ DONE | `pytest backend/tests/ -v` → **87 passed** (thêm `test_model_boot.py` 5 + `test_allocation_cache.py` 3 mới: khớp gọi tay `analyze_run`, cache miss→None, demand>>capacity→dual>0 chứng minh LP thật chạy không phải luôn 0); `python tests/test_invariants.py` (tier-2, repo root) → **9/9 PASS**; smoke test tồn tại không chạm | INT (Bước 5: P3 pricing=elasticity optimizer thật) |
| H+?? | INT | **Bước5 DONE** (P3 pricing=elasticity optimizer thật): `backend/src/pricing/engine.py` v2 — `apply_rules`(YAML)+`load_rules` xoá, thay `apply_dynamic(f0,ctx,bid_total_vnd,elasticity)`: mùa vụ đọc thật từ `models/artifacts/bt5_pricing_params.json::delta_mua` (hè +7.5%/Tết +4.5%, không gõ tay), động gọi `app.elasticity.Elasticity.optimal_price` (max P(mua|r)·(p−c), c=Σbid DLP P2) trong dải `[1−0.05·(1−LF_max), 1+0.15·LF_max]` (tham số từ `app.config.DEFAULT_POLICY`, một nguồn). `elasticity=None` (chưa boot Pricer) ⇒ fallback mùa-vụ-only, không suy đoán hệ số. `PricingEngine.price()` thêm tham số `bid_total_vnd` (mặc định 0, không phá test cũ); `offer/service.py` tính `bid_total` trước rồi truyền vào (thứ tự đổi, hành vi ACCEPT/REJECT giữ nguyên). `routes_offers.py` wire `elasticity=get_pricer().elast` (fail-closed 503 nếu Pricer chưa nạp, đúng invariant cũ). **Giữ nguyên tuyệt đối**: guardrail 5 bước, cap ±5%, held price, CSXH max-sau-cùng, PricingContext≠SafetyContext, tất định, int64 — không đổi 1 dòng nào trong `apply_guardrail`/`csxh_apply`. **A8/A9**: xoá hẳn `backend/rules/pricing_rules.yaml` (kể cả `R_GIO_CHOT`×1.7 cố ý bịa) — không còn cơ chế nhân `he_so` tĩnh. **Bonus fix ngoài scope P3 nhưng bắt buộc để qua CI gate**: `backend/src/allocation/cache.py` (Bước4) có `import pandas` Ở MODULE-LEVEL — vi phạm gate `audit_constants.py` (gate check là substring-match cả file, không phân biệt lazy-import trong hàm) mà Bước4 chưa từng chạy gate để phát hiện; chuyển việc build `pd.DataFrame` sang `integration/ssm_from_postgres.py::build_forecast_df` (hàm mới), `cache.py` giờ 0 literal pandas | ✅ DONE | `pytest backend/tests/ -v` → **87/87 passed** (sửa golden-value 3 chỗ trong `test_api_e2e.py`: giá THO→DHO đổi 456.000đ→**307.000đ** vì bỏ R_GIO_CHOT bịa — không còn chạm trần, `clamped=False`, `violations=[]`; DoD "guardrail clamp thật" nay là `test_guardrail_order_floor_ceiling_delta_round_freeze` gọi thẳng `apply_guardrail`); `python tests/test_invariants.py` (tier-2) → **9/9 PASS**; `python backend/scripts/audit_constants.py` → cả 2 CI gate **OK rỗng** (trước khi fix cache.py: gate `import pandas` FAIL — phát hiện được nhờ chạy lại). Demo case verify tay: đoạn nghẽn (LF_max=0.95) → r=1.14, giá 520.980đ > F0·(1+mùa vụ)=490.200đ; đoạn ế (LF_max=0.10) → r=1.01, giá 462.840đ ≈ F0=456.000đ | FE2 (⚠️ ĐỔI SỐ PITCH: golden THO→DHO 456.000đ→307.000đ, không còn case "chạm trần" trên hero scenario — case vượt trần giờ chỉ còn ở unit test, chưa có scenario Tết phụ sống trên UI, xem cảnh báo dòng dưới), BE1 (review xoá `rules/pricing_rules.yaml`) |
| H+?? | INT | ⚠️ **CẢNH BÁO cho Bước6+**: Bước5 KHÔNG làm phần "scenario phụ Tết chạm trần thật trên S06" (MODEL_BASE_INTEGRATION_PLAN §P3 mục 3, ước lượng gốc ~1-1.5 ngày-người, cần service_run thứ 2 + P1 pipeline sinh seed riêng ngày Tết) — quyết định hoãn vì effort lớn hơn 1 bước nhỏ, giữ đúng nguyên tắc "không bịa case vượt trần". DoD "guardrail clamp thật" tạm thời chỉ có bằng chứng unit test (`apply_guardrail` trực tiếp), CHƯA sống trên offer thật/UI. Docs (`docs/TECHNICAL_OVERVIEW.md` dòng 85/198/359/400, `NOTE_DEV.md`) còn nhắc `rules/pricing_rules.yaml`/`R_MUA_VU_HE2026(×1.075)` — đã xoá file, docs CHƯA cập nhật (backlog docs-manager) | ⚠️ CONTRACT NOTE | Chưa code | FE2 (quyết có cần scenario Tết cho demo hay chấp nhận unit-test-only), docs-manager (đồng bộ TECHNICAL_OVERVIEW.md/NOTE_DEV.md với kiến trúc pricing mới) |
| H+?? | INT | **Bước6 DONE** (P6 — backtest chạy lại với elasticity+DLP gating thật, MODEL_BASE_INTEGRATION_PLAN §P6 mục 2): `backend/src/backtest/engine.py::make_priced_fare_fns` trước đây gọi `PricingEngine(...)` KHÔNG truyền `elasticity` (mặc định None → chỉ mùa-vụ-only, hoàn toàn KHÔNG dùng optimizer P3 dù P3 đã xong ở Bước5) — nay nạp `app.elasticity.Elasticity.load()` (thuần đọc artifact JSON, không cần Postgres/live Pricer) truyền vào `PricingEngine(policy, elasticity=...)`. `replay_aulac`/`aulac_fare` đổi chữ ký thêm `bid_total`+`lf_max` (tính từ state live mỗi request — occupancy thật `1-remaining/N_SEATS`), `engine_.price(f0, ctx, bid_total_vnd=bid_total)` — giá Âu Lạc trong backtest giờ PHẢN ỨNG THEO KHAN HIẾM ĐOẠN thật (trước đó ctx cố định `load_factor_max` mặc định=1.0, không đổi theo state). Bid gating **CHỦ Ý GIỮ NGUYÊN** công thức scarcity đóng (`forecast/bid_price.py`, KHÔNG chuyển DLP `app.bt3_allocation` thật) — ghi rõ lý do trong docstring: LP solve mỗi request × ~2000 event/backtest không khả thi NFR, và cache DLP P2 gắn với Postgres snapshot theo `(service_run_id,matrix_version,forecast_version)`, không có state khớp cho backtest in-memory `SegmentSeatMatrix` — đúng invariant đã ghi trong `CLAUDE.md` ("bid_price.py ... still backs the offline backtest replay ... only, not the live route"), KHÔNG phải thiếu sót. `baseline_fare`/`_default_fare_fn` đổi chữ ký tương thích ngược (`bid_total=0, lf_max=1.0` mặc định) — không phá test cũ gọi `replay_baseline(events, state)` không kèm `fare_fn` | ✅ DONE | `pytest backend/tests/ -v` → **87/87 passed** không đổi; `python tests/test_invariants.py` (tier-2) → **9/9 PASS**; `python backend/scripts/audit_constants.py` → 2 CI gate **OK rỗng**. `python -m src.backtest.engine` (5 seed thật) → baseline median **18.848.000đ** (không đổi — baseline luôn bán F0 niêm yết, không chạm elasticity), Âu Lạc median **23.438.000đ** (trước Bước6: 23.396.000đ khi chưa wire elasticity — đổi rất ít vì `apply_dynamic` fallback trước đó đã dùng đúng mùa vụ thật giống elasticity path trong đa số trường hợp LF vừa phải) = **+24.4%** so baseline (KHÔNG PHẢI +156% cũ, cũng không lệch nhiều so +24.1% cũ trước P3/P6 — số ổn định qua các lần hiệu chỉnh, không có cú nhảy bất thường cần điều tra) | FE2 (cập nhật số S04 nếu pitch còn ghi +24.1%/+156% cũ → **+24.4%**, median 23.438.000đ), BE1 (không đổi checksum committed nào — `backtest_report` checksum không nằm trong `expected_checksums.json`, chỉ event-stream checksum, event stream không đổi) |
| H+?? | INT | **Bước8 DONE** (P5 phần DB/API wiring còn lại, MODEL_BASE_INTEGRATION_PLAN §P5 mục 2-3 — mục 1 port resolver đã DONE ở commit `81d7a19`, mục 4-6 xem cảnh báo dòng dưới): `backend/src/forecast/network.py` += `DWELL_MINUTES` (nguồn: 2 — YAML DGP §1 mang_luoi.ga dwell_phut, khớp golden 8 ga, mỗi dòng có `# nguồn:` để KHÔNG thêm nợ vào `audit_constants.py`). `offer/service.py`: `Offer.seat_plan` đổi `dict`→`list[dict]` (>=2 leg ⇒ ghép ghế), `build_offer` nhận `SeatPlan | MergedSeatPlan`, thêm field `requires_customer_consent`/`change_station_ids`/`so_lan_doi_cho` (mặc định False/[]/0 cho same-seat — additive, không phá offer cũ). `api/schemas.py`: `OfferRequest += priority_passenger: bool=False`, `HoldRequest += consent: bool=False` (⚠️ CONTRACT CHANGE — additive, đã phản ánh vào `openapi.yaml` OfferRequest/OfferResponse/HoldRequest + `ErrorCode` thêm `CONSENT_REQUIRED`). `routes_offers.py`: same-seat rỗng (và không phải `priority_passenger`) → thử `resolver.best_multiseat(..., dwell_minutes=network.DWELL_MINUTES)`; response trả thêm 3 field top-level trên. `state/seat_state_manager.py`: `hold()` cũ giữ nguyên chữ ký (test cũ không sửa), refactor thành wrapper 1 dòng gọi `hold_multi()` mới — verify FREE **toàn bộ** leg trước (pha 1, chưa UPDATE gì), chỉ UPDATE khi mọi leg sạch (pha 2), dùng CHUNG 1 `hold_id` cho mọi cell (schema `seat_hold` vốn không ràng buộc 1 seat_id/hold — **0 migration cần**). `routes_holds.py`: đọc `seat_plan` (giờ luôn là list) → nếu `len>1` mà `consent!=True` → 422 `CONSENT_REQUIRED` (`state/errors.py` thêm class mới) trước khi gọi `hold_multi`. `priority_passenger` API field nối đúng vào bất biến CLAUDE.md "Priority passengers never get a seat-change plan" — test end-to-end qua route (không chỉ unit resolver) | ✅ DONE | `pytest backend/tests/ -v` → **93 passed** (+6 mới: `test_multiseat_offer_requires_consent`/`test_same_seat_offer_no_consent_needed` [offer/service.py thuần, không cần DB], `test_multiseat_hold_all_or_nothing`/`test_multiseat_hold_and_confirm` [Postgres thật — CAS 2-ghế-2-leg atomic, 1 cell fail ⇒ rollback tất cả, seg còn lại vẫn FREE], `test_hold_multiseat_requires_consent` [API thật qua TestClient — thiếu consent 422, kèm consent 201], `test_offer_multiseat_when_same_seat_empty` [API thật — THO→HUE không same-seat nhưng ghép 3 ghế ⇒ 201 + `requires_customer_consent=true`]); sửa `test_offer_no_same_seat_option` cũ dùng `priority_passenger=True` để vẫn dựng được 422 qua API công khai (P5 làm THO→HUE bình thường ACCEPT — hành vi mới, không phải lỗi). `python tests/test_invariants.py` (tier-2) → **9/9 PASS** (gồm `test_uu_tien_khong_ghep`). `python backend/scripts/audit_constants.py` → **59** vi phạm (giảm từ 62 backlog cũ — `DWELL_MINUTES` có nguồn inline nên KHÔNG cộng thêm nợ) | BE1 (review `openapi.yaml`/schema đổi — additive, offer cũ không seat_plan list vẫn tương thích), FE (⚠️ chưa có `web/` — khi dựng FE cần disclosure UI bắt buộc khi `requires_customer_consent=true`, hiển thị `change_station_ids`+`so_lan_doi_cho` trước khi gọi `/holds` với `consent:true`) |
| H+?? | INT | **Docs sync DONE** — cập nhật `docs/API_Contract.md` (viết lại toàn bộ khớp code thật, mọi ví dụ JSON chạy qua `TestClient` thật, không phải minh họa tay: thêm `priority_passenger`/`consent`/`CONSENT_REQUIRED`/mục 3.1.1 ghép nhiều ghế P5; sửa field sai so với code — `recent_decisions.result` không phải `action`, `audit_timeline` là object `{explanation,rules_fired}` không phải mảng, `POST /backtests` không nhận `event_stream_id`, `GET /backtests/{id}.ai_metrics` không có `acceptance_rate` ở tầng tổng hợp, seatmap không có `reused_gap_segments`) và `docs/TECHNICAL_OVERVIEW.md` (test count 48→93; bid-price = DLP thật không còn "công thức xấp xỉu backing route sống"; pricing = mùa vụ thật + elasticity optimizer, `rules/pricing_rules.yaml` đã xóa khỏi mô tả; **số pitch S04 chốt lại: baseline median 18.848.000đ vs Âu Lạc 23.438.000đ = +24,4%** — xóa hẳn "+156%" cũ khỏi doc kỹ thuật (vẫn còn trong `progress.md` lịch sử, không sửa dòng người khác); thêm mục ghép nhiều ghế P5 vào bài toán/luồng/invariant/thuật ngữ; sửa invariant #1 "0 import pandas lúc runtime" thành đúng phạm vi hiện tại (`backend/src/` 0 literal, nhưng pandas CÓ thực thi qua live-import `app.bt3_allocation`); thêm `app/`+`models/`+`integration/` vào cây thư mục §7 (trước đây bị thiếu dù đã load-bearing từ P2)) | ✅ DONE | Đọc lại từng route thật (`routes_demo.py`/`routes_backtests.py`/`routes_offers.py`/`routes_holds.py`) + chạy `TestClient` smoke thật cho offer/hold/confirm/decision/overview/seatmap/analytics/refresh/backtest để lấy JSON thật, không đoán | FE2 (⚠️ **số pitch S04 chốt: +24,4%** — nếu slide/video còn ghi +156% hay +24,1% cũ thì sửa theo số này; golden giá 307.000đ, không còn case chạm trần trên luồng chính) |
| H+?? | INT | ⚠️ **CẢNH BÁO cho P5 phần chưa làm** (mục 4-6 của plan): (4) `dwell_minutes` lấy trực tiếp từ YAML nguồn qua `network.py` (giống pattern `weight` có sẵn) — KHÔNG thêm vào `seed/scenario.json` (không cần, tránh sai lệch 2 nguồn); (5) FE disclosure UI — chưa làm vì **chưa có `web/` frontend scaffolded** (CLAUDE.md xác nhận); (6) scenario phụ demo đổi chỗ trên UI sống — chưa làm (cùng lý do). DoD "test nhóm ưu tiên KHÔNG BAO GIỜ nhận phương án ghép (chạy qua API)" NAY đã có (`test_offer_no_same_seat_option` dùng `priority_passenger=True`), "test consent-required" NAY đã có, "CAS multi-set nguyên tử" NAY đã có, "golden path không đổi hành vi" xác nhận (`test_offer_golden_gap_accept` vẫn pass, seat_plan 1 leg, `requires_customer_consent=False`) | ⚠️ CONTRACT NOTE | Chưa code (4 nằm ngoài scope backend, chờ web/) | FE (khi có web/, cần disclosure UI + gọi `priority_passenger` field cho hành khách ưu tiên) |
| H+?? | INT | **Bước7 DONE** (P4 — forecast runtime = DemandModel, MODEL_BASE_INTEGRATION_PLAN §P4): `backend/src/forecast/forecast.py` viết lại — xoá `pickup_fraction`/`DEFAULT_TARGET_LOAD_FACTOR=0.79`/`DEFAULT_HORIZON_DAYS=90`/số mũ 1.3 (hand-tuned, tự nhận "không phải trích xuất chính xác" trong docstring cũ). `compute_forecast`/`refresh_forecast` giờ dùng cơ chế `app.bt1_forecast.DemandModel.update` thật: `F(u)` = booking curve THẬT đọc trực tiếp từ `models/artifacts/bt1_booking_curves.json` (đọc JSON thuần, KHÔNG qua boot `DemandModel`/Forecaster — tránh phụ thuộc pandas/sklearn/DB boot trong backtest vốn tự chủ theo docstring cũ); `tot_pickup = sold/F(u)` (unconstrain), blend `BLEND_W=0.5` (cùng hằng số `DemandModel.BLEND_W`) với **anchor = seed hiệu chuẩn P1** (`backend/seed/forecast.json`: `sold_tại_seed + forecast_remaining_demand`) — **CHỦ Ý KHÔNG gọi `DemandModel.total()`** (HGB train 22 ga/448 chỗ, lệch grain golden 8 ga/40 chỗ) đúng plan §P4.2 "không claim model HGB dự báo trực tiếp chuyến 40 ghế". `refresh_forecast` thêm bước ghi **divergence log** per-segment (`logging.info`, công thức = `DemandModel.divergence`: `(sold-kỳ_vọng)/kỳ_vọng`) — KHÔNG persist DB (P7.5 drift-monitor/alert là việc sau, ngoài scope P4). `band`/khoảng cách dùng `network.LEG_DISTANCE_KM`+`app.config.BAND_EDGES/LABELS` có sẵn; `tet_window` mặc định False (golden 15/06 không phải Tết). Chữ ký `compute_forecast`/`refresh_forecast` giữ tương thích — `backend/src/backtest/engine.py::_forecast_snapshot` (dùng chung `compute_forecast` cho forecast trong replay) không cần sửa | ✅ DONE | `pytest backend/tests/ -v` → **87/87 passed** không đổi; `python tests/test_invariants.py` (tier-2) → **9/9 PASS**; `python backend/scripts/audit_constants.py` → 2 CI gate **OK rỗng**. Smoke tay qua `TestClient` (lifespan thật): reset→offer(forecast_version=1)→hold→confirm(giá 307.000đ không đổi)→refresh lần 1 (v1→v2, log `forecast divergence seg=4 ... expected=28.2 actual=12 divergence=-57%` — bán chậm hơn kỳ vọng đúng thật vì mới có 1 vé xác nhận)→refresh lần 2 (v2→v3, giữ nguyên `service_run_id`/`che_do_gia`)→offer mới dùng `forecast_version=3` (bất biến 4-version giữ nguyên) | FE2 (số forecast/S0x hiển thị đổi nếu demo trước đó dựa vào `pickup_fraction` cũ — về bản chất gần giống, không phải đổi lớn như P3/P6), BE1 (không đổi schema `demand_forecast` table/route, chỉ đổi công thức nội bộ `forecast.py`) |
| H+?? | INT | **P7 DONE** (P7.1-P7.6, MODEL_BASE_INTEGRATION_PLAN §P7 — tính năng vận hành, sau khi xác nhận P0-P6 đã DONE thật qua code+log ở trên): (P7.1) `backend/src/audit/log.py` — `persist()` ghi mọi `_log`/ProposalLog vào bảng mới `proposal_log` (V3 migration), dùng chung bởi mọi mục dưới. (P7.2) `backend/src/allocation/reallocation.py` — `propose()` re-solve DLP (tái dùng `allocation/cache.py` P2, KHÔNG giải LP lần 2) rồi diff `booking_limit` cũ/mới (đoạn×loại_hành_trình×lớp_ghế) → `quota_version` (V3 migration) trạng thái PENDING; `approve/reject/rollback` qua route mới `routes_allocation.py` (`POST /allocation/refresh|{v}/approve|reject|rollback`, role gate `X-Actor-Role` — ponytail: header check thô, KHÔNG phải RBAC/JWT thật). CHƯA enforce `booking_limit` trong `/offers` (chỉ workflow đề xuất/duyệt/rollback — enforce thật để riêng, cần regression kỹ hơn golden path). (P7.3) `backend/src/waitlist/service.py` — Postgres-backed trên bảng `waiting_list` có sẵn từ V1 (V4 migration bổ sung `priority_passenger`/`quantity`/`matched_hold_id`); điểm ưu tiên TÁI DÙNG đúng công thức `app.waitlist.WaitlistManager` (không viết lại lần 2); `match()` gọi thẳng `routes_offers.create_offer`+`routes_holds.create_hold` (tái dùng nguyên pipeline giá/CAS, không viết pipeline thứ hai) → khớp được thì tạo **Hold thật** (giữ chỗ tạm để khách thanh toán trong TTL 10 phút bình thường) — đây là gap còn thiếu ngay ở tier-2 `app/waitlist.py` (chưa gọi `hold_with_expiry`), P7.3 vá luôn ở tầng runtime. Route `POST /waitlist`, `GET /waitlist`, `POST /waitlist/match` (ponytail: không có worker nền, `match` là ops-trigger tường minh). (P7.4) `backend/src/group/service.py` — bọc `app.group_seating.plan_group` (CP-SAT/fallback greedy, tái dùng shim `integration/ssm_from_postgres.py` của P2, không viết converter ma trận thứ 2), route `POST /group/quote`. (P7.5) `backend/src/forecast/forecast.py::refresh_forecast` — divergence trước đây tính rồi CHỈ log (`_logger.info`) rồi bỏ (Bước7); nay trả kèm `drift{divergence_by_segment,alerts,threshold}` (ngưỡng tái dùng `app.reallocation.DIV_THRESHOLD`), `routes_demo.py` persist vào `proposal_log` + `GET /demo/overview` trả `drift_alerts`. (P7.6) `backend/src/offer/override.py` — `POST /offers/{id}/override` ghi đè giá TRONG guardrail [floor,ceiling]·F0 (`GuardrailViolation` 422 nếu vượt), chặn nếu offer đã có hold ACTIVE/CONFIRMED (`SeatConflict` 409 — giá đã khoá bất khả xâm phạm), role gate như P7.2; V5 migration thêm `offer.origin_station_id/dest_station_id/seat_class` (cần để tra đúng `fare_product` — offer trước đó không lưu O-D riêng, chỉ có trong `seat_plan` JSONB dạng seat_id/segment). **Fix phát sinh bắt buộc**: `reset_scenario()` (state/seat_state_manager.py) trước đây KHÔNG dọn `waiting_list`/`quota_version`/`proposal_log` theo `service_run_id` — waitlist/quota cũ tồn đọng qua các lần reset làm `match()` xử lý nhầm entry của phiên trước; đã thêm 3 câu `DELETE ... WHERE service_run_id=%s` vào cùng transaction reset | ✅ DONE | `pytest backend/tests/ -q` → **110 passed** (từ 93 baseline: +1 `test_audit.py`, +2 `test_drift_monitor.py`, +2 `test_group_seating.py`, +3 `test_waitlist.py`, +5 `test_reallocation.py`, +4 `test_override.py`); V3/V4/V5 migration áp qua `docker compose up flyway` thành công, không đổi hành vi 11 route cũ (golden path/backtest/decision không đổi) | BE1 (review 3 migration mới + 6 route mới — chưa vào `openapi.yaml`/`docs/API_Contract.md`, chỉ có trong route/schema code), FE (chưa có UI cho waitlist/group/allocation-approval/override — vẫn đúng tình trạng "chưa có `web/`" đã ghi từ Bước6) |
| H+?? | INT | **P8 DONE** (sweep số, tiếp backlog 59 literal từ P0.6/P8 cũ — MODEL_BASE_INTEGRATION_PLAN §P8): `backend/scripts/audit_constants.py` cải tiến — thêm exemption cho literal dùng làm **chỉ số subscript/slice** (`row[4]`, `.hex[:12]`) qua AST (`ast.Subscript.slice`/`ast.Slice.lower|upper|step`), vì đây không phải hằng số nghiệp vụ mà chỉ là vị trí truy cập dữ liệu — loại bỏ ngay 30/74 vi phạm là noise thuần (không phải số bịa); mở rộng WHITELIST thêm `4` (chữ số thập phân làm tròn chuẩn repo, `round(x,4)`) + `202/403/500` (HTTP code còn thiếu, cùng category đã whitelist sẵn 200/201/400/404/409/410/422/503). 44 vi phạm còn lại (gồm cả 8 dòng P7 mới tạo) gắn `# nguồn:`/`# spec`/`# ponytail:` thủ công theo đúng nguồn thật: `forecast/network.py` (km/weight → nguồn 2 YAML mang_luoi.ga / nguồn 1 neo SGO), `forecast/bid_price.py` (anchor → nguồn 1; p_low/p_high → ponytail, module chỉ còn dùng cho backtest không phải live route), `forecast/forecast.py` (BLEND_W → nguồn 4 app.bt1_forecast; 1e-4/1e-6 → ponytail numeric guard; confidence formula → ponytail), `backtest/events.py` (5 seed dates → spec; band threshold 900/300 → nguồn 2 khớp app.config.BAND_EDGES), `backtest/engine.py` (GOLDEN_SEAT_IDX/FULL_BOOKED_SEATS → spec), `offer/service.py`/`state/seat_state_manager.py`/`pricing/context.py` (TTL 300s/600s → spec API_Contract; sentinel 999 → ponytail), `api/schemas.py`/`api/routes_offers.py` (U_FORECAST 14.0 → nguồn 4; PEAK_SUMMER dates → spec Master §1.1), `waitlist/service.py` (đặt tên hằng `SCORE_SCALE`/`DEFAULT_MAX_MATCHES` thay literal rải rác, tag ponytail) | ✅ DONE | `python backend/scripts/audit_constants.py` → 2 CI gate **OK rỗng** + sweep literal **0 vi phạm** (từ 74, gồm cả literal P7 mới); `pytest backend/tests/ -q` → **110 passed** không đổi sau khi sửa script + thêm comment (không đụng logic runtime) | Cả đội (backlog P8 cũ chính thức đóng — mọi literal trong `backend/src/` giờ có nguồn hoặc thuộc whitelist kỹ thuật) |
| H+?? | INT | **Docs sync P7 DONE**: `backend/openapi.yaml` += 9 path mới (`/allocation/refresh|{version}|{version}/approve|reject|rollback`, `/waitlist` GET+POST, `/waitlist/match`, `/group/quote`, `/offers/{offer_id}/override`) + 14 schema mới (`QuotaRow`/`QuotaVersionResponse`/`AllocationDecisionRequest`/`WaitlistAddRequest`/`WaitlistAddResponse`/`WaitlistEntry`/`WaitlistListResponse`/`WaitlistMatchResponse`/`GroupQuoteRequest`/`GroupQuoteResponse`/`OverrideRequest`/`OverrideResponse`) + `ErrorCode` thêm `RESOURCE_NOT_FOUND`/`FORBIDDEN`/`GUARDRAIL_VIOLATION` (RESOURCE_NOT_FOUND vốn đã dùng bởi `/backtests`/`/decisions` nhưng thiếu trong enum — tiện tay bổ sung) + parameter `ActorRole` (header `X-Actor-Role`) + response `Forbidden` (403); `docs/API_Contract.md` += §3.4 (override) + §6 (phân bổ & hạn mức) + §7 (hàng chờ) + §8 (xếp nhóm), mọi ví dụ JSON lấy từ `TestClient` thật (không phải minh hoạ tay, đúng chuẩn file đã có) — bảng quota 63 dòng rút gọn còn vài dòng đại diện cho dễ đọc, ghi rõ tổng số dòng thật | ✅ DONE | `python -c "import yaml; yaml.safe_load(open('backend/openapi.yaml'))"` → parse OK, 20 path/28 schema; `pytest backend/tests/ -q` → **110 passed** không đổi (docs-only, không đụng code) | BE1 (review 2 file docs mới), FE (khi có `web/`, 4 nhóm endpoint mới đã có contract sẵn dùng) |
| H+?? | INT | **Fix: cache DLP stale sau hold/expire** (phát hiện khi soạn ví dụ output thật cho docs sync P7 — waitlist `match()` tự tạo hold → `matrix_version` bump nhưng `allocation_cache` không tự refresh, offer kế tiếp cache miss → `503 POLICY_UNAVAILABLE` giả dù DLP giải được bình thường; gốc rễ sâu hơn "lỗi vặt": bid price cache cũng STALE theo sức chứa cũ, không chỉ thiếu). `routes_offers.py` bước 5: cache miss → gọi `allocation_cache.refresh(service_run_id)` 1 lần rồi thử `get()` lại (refresh tự đọc seatmap/forecast MỚI NHẤT từ DB nên khớp đúng `matrix_version`/`forecast_version` đã snapshot ở bước 1) → 503 chỉ còn xảy ra khi LP thật sự fail. Cân nhắc phương án đẩy (refresh chủ động trong `hold_multi`/`expire_due_holds`) nhưng chọn phương án kéo (lazy trong route) để không phá ranh giới sở hữu `state/` (thuần CAS, không biết gì về DLP) — không đổi `GET /demo/analytics` (đọc-only, cố ý hiển thị 0 khi cache miss, không tự recompute) | ✅ DONE | Test mới `test_allocation_cache_staleness.py::test_offer_survives_matrix_version_bump_from_hold` (hold rồi offer ngay, matrix_version đã đổi → vẫn 201 ACCEPT); tái hiện đúng kịch bản gốc (`waitlist add→match→offer` liên tiếp) bằng tay qua `TestClient` → `201 ACCEPT` (trước fix: `503`); `pytest backend/tests/ -q` → **111 passed**; `audit_constants.py` → 2 CI gate + sweep vẫn **0 vi phạm** | Cả đội (đã sửa 1 trong các gap ghi ở dòng P7 DONE phía trên) |
| H+?? | FE (Claude) | DEV7_PHASED Pha 0 — bring-up: `docker compose up -d --build` (db/flyway/backend) trên compose hiện có (không sửa file); DB đang là mock-load 1 tháng (theo CẢNH BÁO SUSPEND trong CLAUDE.md), gọi `POST /demo/scenarios/SE1_2026-06-15_LE/reset` để đưa golden scenario trở lại; `npm run dev` cho `web/` (đã có sẵn `.env.local` đúng `API_SERVER_URL`) | ✅ DONE | `curl localhost:8000/api/v1/demo/overview?service_run_id=SE1_2026-06-15_LE` → JSON thật (occupancy 0.786, bottlenecks seg2/3/4, underused seg1/4/5); cả 5 route `/admin/{overview,seat-matrix,analytics,backtest,decisions/[id]}` qua proxy Next.js (`localhost:3000`) → HTTP 200, proxy `/api/v1/*`→backend xác nhận qua curl khớp response backend gốc | Pha 1–6 (dev server + DB đã sẵn sàng để test) |
| H+?? | FE (Claude) | DEV7_PHASED Pha 1 — verify Dashboard `/admin/overview` (`web/app/ops/page.tsx`) theo checklist DEV7 §2: đã đọc code, KHÔNG cần sửa gì — cả 5 mục đã đúng sẵn (5 KPI card đúng field, dải tải L1-L7 tô màu theo occupancy, `bottlenecks`/`underused` map `segment_id`→tên ga qua `segmentLabel()` (`current-run.tsx`, tra từ `GET /demo/runs/{id}/stops` sống, không hardcode), bảng quyết định dùng field `result` (không phải `action`) + link đúng `/admin/decisions/{id}`, có empty-state cho cả 2 list rỗng) | ✅ DONE | Đọc `web/app/ops/page.tsx` + `web/src/lib/current-run.tsx` + `web/src/api/{types,client}.ts` đối chiếu từng dòng với checklist; số liệu sống khớp: KPI card sẽ hiện occupancy 78.6%, bottlenecks seg 2/3/4, tên ga lấy từ `/demo/runs/SE1_2026-06-15_LE/stops` (Hà Nội/Phủ Lý/...) — verify qua curl trực tiếp API, không qua trình duyệt (ghi chú: chưa chụp screenshot UI thật) | Pha 2-6 (không có thay đổi contract/field nào cần báo) |
| H+?? | FE (Claude) | DEV7_PHASED Pha 3 — verify Dự báo–Phân bổ `/admin/analytics` (`web/app/forecast/page.tsx`, re-export tại `app/admin/analytics/page.tsx`) theo checklist DEV7 §4: đọc code, 3 tab đúng field (`forecasts[].{forecast_remaining,confidence}` với `confidence==null`→"—" + badge màu theo ngưỡng .8/.6; `segment_loads[].{occupancy,remaining_capacity}`; `allocations[].bid_price_vnd`). Sửa 1 dòng: subtitle tab Bid-price trước chỉ nhắc "cache miss", chưa nhắc case "đoạn không nghẽn" — hai trạng thái bid=0 khác nhau mà checklist yêu cầu đừng gộp — nay ghi rõ cả 2 lý do | ✅ DONE | Backend DB rỗng data (reset trước đó không còn hiệu lực) → `POST /demo/scenarios/SE1_2026-06-15_LE/reset` lại rồi `curl .../demo/analytics?service_run_id=...` xác nhận số thật: forecasts 7 đoạn (conf 0.74), segment_loads occ 0.65–0.975, allocations seg1-3 bid>0 (109k/62k/133k) + **seg4-7 bid=0 đúng thiết kế** (golden gap THO→DHO seg4 remaining=11 > demand=4 ⇒ không nghẽn, không phải cache miss); `curl localhost:3000/admin/analytics?tab={demand,load,allocation}` → cả 3 HTTP 200, SSR shell render tiêu đề đúng; URL `?tab=` giữ state qua `router.replace` (không mất khi đổi tab) | Pha 4-6 (không đổi contract/field) |
| H+?? | FE (Claude) | DEV7_PHASED Pha 4 — verify AI-suggest giá `/admin/decisions/{id}` (`web/app/decisions/[decisionId]/page.tsx`): checklist label/rules_fired-order/dùng thẳng `d.action` đã đúng sẵn, KHÔNG cần sửa. Nhưng phát hiện **bug thật khi lấy 1 `decision_id` thật từ Pha 1** (`GET /decisions/D-T-RUN:SUM26-SE7:2026-06-30-001017`): 100% (557.827/557.827 hàng, kiểm bằng SQL trực tiếp) `decision_record` hiện có (nạp từ mock dataset, không qua pipeline `/offers` sống) có `audit_timeline=NULL`, `bid_price_breakdown=NULL`, `versions={matrix,forecast,policy}` (không có hậu tố `_version`) — trang cũ gọi `d.audit_timeline.explanation`/`Object.entries(d.bid_price_breakdown)` sẽ crash `TypeError` với MỌI decision thật bấm từ Pha 1. Sửa tại chỗ (file FE sở hữu, không đụng backend theo Bẫy #3): optional-chain + fallback text cho `audit_timeline`, `Object.entries(x??{})` cho bid breakdown, `versions.matrix_version??versions.matrix` (và forecast/policy tương tự) cho cả 2 hình dạng key; cập nhật type `DecisionDetailData` (`web/src/api/types.ts`) cho đúng nullable/2 hình dạng | ✅ DONE | `npx tsc --noEmit` sạch; self-check `node test_decision_page.mjs` chạy đúng logic trang trên JSON thật của decision null-shape ở trên → không throw, fallback đúng (`matrixV/forecastV/policyV=1`, `bids=[]`, `rulesFired=[]`, explanation có câu chú thích mock) | Pha 5-6 (không đổi contract; ghi chú cho BE1/INT: nếu sau này muốn `audit_timeline`/`bid_price_breakdown` có dữ liệu thật cho quyết định mock, cần sửa `load_mock_from_dataset.py`, ngoài scope DEV7) |

<!-- Append dòng mới ngay dưới đây. Ví dụ:
| H+02 | BE1 | contract freeze v1.0 | ✅ DONE | `git show a1b2c3` · openapi.yaml 8 endpoints + canonical examples | BE2,BE3,FE1,FE2 |
| H+03 | BE1 | seed/ prior commit | ✅ DONE | `git show d4e5f6` · 7 file · golden gap C01-S017 verified | BE2,BE3,FE1,FE2 |
| H+06 | BE3 | continuous_same_seat | ✅ DONE | `pytest tests/test_merging.py -q` → 8 passed | FE1 (S02) |
| H+07 | BE1 | atomic hold CAS | ⛔ BLOCKED | chờ seed/scenario.json schema | chờ chính mình xong seed/ |
-->

| H+?? | — | Kickoff `web/` (chưa scaffold): chia 2 dev — DEV6 (duyệt chặng ngắn A/B **+ đặt vé user**, cần backend mới `review/`; 2 màn gộp về 1 người vì phụ thuộc lẫn nhau, không cần hợp đồng liên-dev) và DEV7 (dashboard/seatmap/forecast/pricing/strategy — thuần read-only, API có sẵn, không viết backend) | 🚧 WIP | `plan/DEV6_FE_SHORT_LEG_REVIEW.md`, `plan/DEV7_FE_SCREENS_BOOKING.md` | DEV6, DEV7 |

---

## Bảng checkpoint (BE1 cập nhật tại mỗi mốc)

| Mốc | Điều kiện | Xác nhận | Đạt? |
|---|---|---|---|
| H+02 | OpenAPI + `seed/` schema versioned; 0 câu hỏi P0 mở | BE1 + cả đội | ☐ |
| H+03 | `seed/` commit vào git (dù mới là prior) | BE1 | ☐ |
| H+06 | Fixture happy path `offer→hold→confirm` chạy trong UI | BE1 + FE1 + FE2 | ☐ |
| H+10 | Core transaction / resolver / pricing / metrics tests xanh | BE1–BE3 | ☐ |
| H+14 | **Real golden path end-to-end** | Cả đội | ☐ |
| H+18 | P0 feature complete; release candidate; **FEATURE FREEZE** | BE1 | ☐ |
| H+23 | Smoke 3/3; p95 / error / a11y evidence | BE1 + FE1 | ☐ |
| H+26 | **Video backup** + pitch evidence | FE2 | ☐ |
| H+30 | Submission checksum khớp | BE1 + FE2 | ☐ |

---

## Definition of Done (tick khi có bằng chứng — Master §9)

- [x] Reset deterministic — cùng seed ⇒ cùng checksum (smoke 3/3, checksum `8c8ef3c7…` giống nhau)
- [x] Baseline **từ chối** golden request `THO→DHO` (`test_baseline_rejects_golden_request` ⭐ pass)
- [x] Âu Lạc tìm **đúng** same-seat gap trên `C01-S017` (leg L3+L4) (smoke E2E: seat_plan C01-S017 seg[3,4] reused_gap=true)
- [x] Offer hiển thị price / bid / versions / expiry (API trả đủ 3 mức giá + bid/leg + 4 versions + expires_at; phần "hiển thị" chờ FE)
- [x] Hold nguyên tử — 2 hold cạnh tranh: 1 OK, 1 → 409, **0 partial hold** (`test_two_competing_holds_one_wins` pass trong 48/48)
- [x] Guardrail clamp thật (có case vượt ceiling) (rule `R_GIO_CHOT` ×1.7 → golden path `violations=['TRAN']`, clamp về 1.6×F0)
- [x] Backtest ≥5 seed — median + min/max + raw; failed seed **không bị giấu** — ĐÃ CHỐT giá thật T5: baseline 19.531.000đ vs Âu Lạc 49.958.000đ
- [x] Heatmap cập nhật sau confirm (seatmap API: C01-S017 seg 3,4 FREE→SOLD sau confirm; UI heatmap chờ FE)
- [x] Decision truy vết được (versions + rule đã bắn + violations) (`GET /decisions/{id}` trả input_hash+versions+rules_fired+violations+explanation)
- [x] Smoke test **3/3**, mỗi lần **< 90 giây** (API-level 3/3, tổng 1.6s; smoke qua UI chờ FE)
- [ ] **0 vi phạm** sàn/trần + CSXH, hiển thị trên S06 (backend enforce + ghi violations; màn S06 chưa có — FE1)
- [ ] Video backup + AI collaboration log sẵn sàng (FE2)
- [x] `grep -r "_ground_truth" src/` **rỗng**
- [x] NFR: offer p95 < 1s · resolver < 200ms · reset < 3s (đo thật: offer 0.09–0.15s, reset ~0.2s, resolver <200ms test BE3)

---

## Stop-rules (không thương lượng)

| Điều kiện | Hành động |
|---|---|
| H+02 còn open P0 contract question | **Dừng code core.** Chốt canonical examples trước. |
| H+18 golden path chưa 3/3 | **Không làm P1/P2.** Giữ static evidence. |
| H+23 smoke fail | Chuyển sang video backup. |
| H+26–28 | Fix **tối đa 1** blocker. Mọi thay đổi chạy lại smoke. |
| H+28–30 | **Cấm refactor. Cấm upgrade dependency.** |

---

## Ghi chú / quyết định phát sinh

Ghi lại quyết định lệch khỏi plan + lý do (để pitch trả lời được, và để người sau hiểu):

| Giờ | Dev | Quyết định | Lý do |
|---|---|---|---|
| H+?? | BE2 | Đã commit thẳng `seed/backtest/events-seed-*.jsonl` + `checksums.json` vào `seed/backtest/` thay vì "nộp PR cho BE1" | Làm việc độc lập/song song một mình trong phiên này, chưa có BE1 để duyệt PR. **BE1 cần rà lại khi bắt đầu** — schema event: `{request_id, seed, origin, dest, segment_from, segment_to, seat_class, quantity, distance_km, days_to_departure}`, sort theo `days_to_departure` giảm dần (thời gian thực tăng dần) |
| H+?? | BE2 | Backtest engine dùng state RIÊNG (`SegmentSeatMatrix` nhẹ, không phụ thuộc pandas/CSV) thay vì SSM sản xuất của BE1 | `demo/ssm/seat_state_matrix.py` cần `stations.csv`/`trains.csv` từ dataset 4GB (chưa có) + pandas (chưa cài trong venv này). Backtest cần replay hàng trăm request/seed nhanh trong bộ nhớ — tách state là hợp lệ theo Master Plan (backtest ≠ live API state). Cùng semantics TRONG=0/DA_BAN=1 để không lệch định nghĩa |
| H+?? | BE2 | `seed/forecast.json` schema tự chốt (chưa đối chiếu BE1) | BE1 chưa có mặt trong phiên. Schema: `{forecast_version, service_run_id, che_do_gia, days_to_departure, segments:[{segment_id, forecast_remaining, confidence}]}` — khớp API_Contract.md §2.5 `forecasts[]` ở mức khái niệm nhưng grain là PER-SEGMENT (leg) chứ không phải per-O-D, đúng yêu cầu DEV2 doc. **BE1 xác nhận lại khi rảnh** |
| H+?? | BE2 | ⚠️ **CONTRACT CHANGE**: `src/forecast/`, `src/backtest/`, `tests/` chuyển từ gốc repo (như 00_MASTER_PLAN.md §5.1 ghi) vào **`backend/src/`**, **`backend/tests/`** | `backend/docker-compose.yml` build service `backend` với `context: backend/` (Dockerfile `COPY . .` chỉ copy trong `backend/`) — nếu `src/` nằm ở gốc repo, container backend **không thấy code**. Đã sửa: di chuyển `src/` + `tests/` vào `backend/`, sửa lại `parents[N]` trong `src/backtest/events.py` để vẫn trỏ đúng `generated_data/` và `seed/` ở gốc repo (seed/ KHÔNG di chuyển — FE cũng đọc trực tiếp seed/ làm mock data theo DEV4 plan). Test lại: `cd backend && python -m unittest discover -s tests -t .` → 11 passed | **BE1 cần cập nhật Master Plan §5.1** (đổi `src/...` → `backend/src/...`) và báo BE3 (`src/merging/`, `src/pricing/`, `src/offer/`) + BE1 (`src/state/`) đặt code cùng dưới `backend/src/` cho khớp Docker context |
| H+?? | BE2 | ⛔ **CẢNH BÁO cho BE1** (chưa sửa — `docker-compose.yml` là file BE1 sở hữu, BE2 không tự đụng): `seed/` (kể cả `seed/backtest/`) vẫn ở gốc repo, đúng ý Master Plan (FE đọc trực tiếp làm mock data), nhưng `backend/docker-compose.yml` hiện **không có volume nào mount `seed/` vào container** `backend`. Nếu `POST /demo/scenarios/{id}/reset` hoặc `POST /backtests` đọc file `seed/*` từ đĩa lúc runtime, container sẽ không thấy — cùng loại lỗi context như `src/` (đã fix ở dòng trên) | BE1 cần tự quyết lúc cắm API thật: (a) thêm `volumes: - ../seed:/app/seed:ro` vào service `backend` trong `docker-compose.yml` (đơn giản nhất, `seed/` chỉ ~50KB), hoặc (b) `scripts/extract_seed.py` nạp thẳng vào Postgres qua port 5432 lúc offline, API reset không cần đọc file `seed/` lúc runtime nữa |
| H+?? | BE2 | Cần BE1 gộp checksum khi bắt đầu: tôi chỉ tạo `seed/backtest/checksums.json` (scope riêng backtest, cố tình không đụng file tổng), nhưng `DEV1_BE_STATE_INTEGRATION.md` dòng 217 có test `test_seed_package_matches_expected_checksums()` — đối chiếu **toàn bộ** `seed/` với `seed/expected_checksums.json` (file tổng, BE1 sở hữu, chưa tồn tại) | BE1 cần đọc `seed/backtest/checksums.json` và gộp 5 giá trị sha256 đó vào `expected_checksums.json` khi dựng file manifest tổng, nếu không `test_seed_package_matches_expected_checksums()` sẽ thiếu phần backtest |
