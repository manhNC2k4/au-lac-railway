# Địa chỉ backend mà Next.js server sẽ proxy tới.
API_SERVER_URL=http://127.0.0.1:8000

# Tùy chọn: chỉ đặt khi API cho phép trình duyệt gọi trực tiếp (đã bật CORS).
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com
